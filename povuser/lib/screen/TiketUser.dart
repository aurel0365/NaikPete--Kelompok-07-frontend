import 'package:flutter/material.dart';

class TiketUser extends StatelessWidget {
  const TiketUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Layar tiket'),
    );
  }
}
